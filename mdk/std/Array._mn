module std;
// TODO!
/*


//@ Represents an array of type T.
public class Array<T> {
    public constructor<T>(int size) {
        _native = new std::native::ArrayInterface(T.Name, size);
    }

    //@ The native representation of the array.
    private std::native::ArrayInterface _native;
}

*/